{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Minesweeper",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/minesweeper.exe",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [
        {
          "name": "PATH",
          "value": "C:/msys64/ucrt64/bin;${env:PATH}"
        }
      ],
      "externalConsole": true,
      "MIMode": "gdb",
      "miDebuggerPath": "C:/msys64/ucrt64/bin/gdb.exe",
      "preLaunchTask": "Build Minesweeper"
    }
  ]
}