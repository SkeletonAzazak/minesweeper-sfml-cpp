#pragma once

// Розмір комірки
constexpr unsigned char CELL_SIZE = 8;

// Кількість колонок
constexpr unsigned char COLUMNS = 35;

// Кількість рядків
constexpr unsigned char ROWS = 35;

// Зміна розміру вікна програми
constexpr unsigned char SCREEN_RESIZE = 3;

// Кількість мін
constexpr unsigned short MINES = 150;

// Висота шрифту
constexpr unsigned char FONT_HEIGHT = 16;

// Ширина шрифту
constexpr unsigned char FONT_WIDTH = 8;

// Тривалість кадру
constexpr unsigned FRAME_DURATION = 16667;

// Тривалість ефекту
constexpr unsigned char EFFECT_DURATION = 16;